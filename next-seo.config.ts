const description =
  "INTERFERENCE!";
const title = "INTERFERENCE!";
const url = "https://intereference.store";

const seo = {
  title,
  description,
  openGraph: {
    description,
    title,
    type: "website",
    url,
  },
};

export { seo as defaultSEO, url as defaultUrl };
